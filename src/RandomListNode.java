public class RandomListNode {
    int label;
    RandomListNode next = null; //指向下一个节点
    RandomListNode random = null; // 指向任意一个节点

    RandomListNode(int label) {
        this.label = label;
    }
}
